// types.ts (or wherever you define types)
export interface Bin {
  name: string;
  depth: number;
  width: number;
  length: number;
  noBins: number;
}

export interface Container {
  name: string;
  capacity: string;
}
